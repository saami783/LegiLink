{% extends 'components/user/sidenav.html.twig' %}

{% block title %}Hello HomeController!{% endblock %}

{% block body %}

    <p> {{ notifaction.id }}</p>
    <p> {{ notifaction.message }}</p>
    <p> {{ notifaction.isNew }}</p>
    <p> {{ notifaction.createdAt|date("d/m/Y " ~ "à" ~ " H:i:s") }} </p>
{% endblock %}